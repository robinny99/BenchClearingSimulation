using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IsKnockBack : MonoBehaviour
{
    /*public Animator playerAnimator;
    private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.tag == "enemy" || other.gameObject.tag == "Ball" || other.gameObject.tag == "Bat")
        {
            Debug.Log("플레이어에 닿은 오브젝트 이름 : " + other.gameObject.name);
            playerAnimator.SetTrigger("IsKnockBack");
        }

        if (other.gameObject.tag == "Blue")
        {
            playerAnimator.SetTrigger("aaa");
        }
    }*/
}

